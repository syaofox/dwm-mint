backend = "glx";
use-damage = true;
vsync = true;

fading = false;
shadow = false;

inactive-opacity = 1.0;
active-opacity = 1.0;
frame-opacity = 1.0;
inactive-opacity-override = false;

opacity-rule = [
    "100:class_g = 'org.wezfurlong.wezterm' && fullscreen",
    "95:class_g = 'org.wezfurlong.wezterm' && !fullscreen",
    "100:class_g = 'Xfce4-terminal' && fullscreen",
    "95:class_g = 'Xfce4-terminal' && !fullscreen",
    "100:class_g = 'kitty' && fullscreen",
    "95:class_g = 'kitty' && !fullscreen",
    "98:class_g = 'Rofi' && !fullscreen"
];


use-ewmh-active-win = true;
mark-wmwin-focused = true;
mark-ovredir-focused = false;
detect-rounded-corners = false;
detect-client-opacity = false;
detect-transient = false;
log-level = "warn";

wintypes:
{
  tooltip = { fade = false; shadow = false; opacity = 1.0; };
  dock = { shadow = false; }
  dnd = { shadow = false; }
  popup_menu = { opacity = 1.0; }
  dropdown_menu = { opacity = 1.0; }
};
