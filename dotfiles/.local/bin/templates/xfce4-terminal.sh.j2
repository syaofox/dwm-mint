#!/bin/bash
# Auto-generated by generate-app-themes.py
xfconf-query -c xfce4-terminal -p /color-foreground -s '{{ FG }}'
xfconf-query -c xfce4-terminal -p /color-background -s '{{ BG }}'
xfconf-query -c xfce4-terminal -p /color-cursor -s '{{ CURSOR_BG }}'
xfconf-query -c xfce4-terminal -p /color-cursor-foreground -s '{{ BG }}'
xfconf-query -c xfce4-terminal -p /color-cursor-use-default -s false
xfconf-query -c xfce4-terminal -p /color-selection -s '{{ FG }}'
xfconf-query -c xfce4-terminal -p /color-selection-background -s '{{ SEL_BG }}'
xfconf-query -c xfce4-terminal -p /color-selection-use-default -s false
xfconf-query -c xfce4-terminal -p /color-palette -s '{{ BLACK }};{{ RED }};{{ GREEN }};{{ YELLOW }};{{ BLUE }};{{ MAGENTA }};{{ CYAN }};{{ WHITE }};{{ BLACK }};{{ RED }};{{ GREEN }};{{ YELLOW }};{{ BLUE }};{{ MAGENTA }};{{ CYAN }};{{ WHITE }}'
xfconf-query -c xfce4-terminal -p /color-use-theme -s false

xfconf-query -c xfce4-terminal -p /font-name -s 'UbuntuMono Nerd Font Mono 12'
xfconf-query -c xfce4-terminal -p /font-use-system -s false

xfconf-query -c xfce4-terminal -p /misc-cursor-shape -s TERMINAL_CURSOR_SHAPE_UNDERLINE
xfconf-query -c xfce4-terminal -p /misc-cursor-blinks -s true

xfconf-query -c xfce4-terminal -p /scrolling-bar -s TERMINAL_SCROLLBAR_NONE

xfconf-query -c xfce4-terminal -p /misc-borders-default -s false
xfconf-query -c xfce4-terminal -p /misc-menubar-default -s false
xfconf-query -c xfce4-terminal -p /misc-copy-on-select -s true

xfconf-query -c xfce4-terminal -p /text-blink-mode -s TERMINAL_TEXT_BLINK_MODE_FOCUSED
xfconf-query -c xfce4-terminal -p /background-mode -s TERMINAL_BACKGROUND_TRANSPARENT
xfconf-query -c xfce4-terminal -p /background-darkness -s 0.900000
xfconf-query -c xfce4-terminal -p /color-background-vary -s false
